//! Modal dialogs beyond the simple name-input/confirm windows already
//! inlined in `panels::collections` (e.g. import/export wizards, the
//! environment editor, run configuration). Empty for now — wave C adds
//! content here.
